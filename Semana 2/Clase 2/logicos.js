// Derecho a votar
let edad = 20;
let tieneDui = false;

let puedeVotar = (edad >= 18 && tieneDui) ? "Puede votar" : "No puede votar";
console.log(puedeVotar);

//  Descuento condicionado

let age = 35;
let esMiembro = false

let tieneDescuento = (age >= 30 || esMiembro) ? "Se le aplica descuento" : "No se le aplica descuento"
console.log(tieneDescuento)

// Subir a
let años = 12;
let height = 1.90;

let puedeSubir = (años >= 12 && height >= 1.60) ? "Se puede subir al juego" : "No puede subir"
console.log(puedeSubir)

